program: src.detection.train_yolo
entity: "c5-team1"
project: "c6_yolo"
method: grid
metric:
  name: val_AP50
  goal: maximize
parameters:
  epochs:
    value: 30
  batch_size:
    value: 16
  data_dir:
    value: "src/detection/dataset/sequential/fold_1" # Change per fold
  freeze_strategy:
    values: [1, 2, 3]
  lr:
    values: [0.01, 0.001, 0.0001, 0.00001]
command:
  - ${env}
  - ${interpreter}
  - "-m"
  - ${program}
  - ${args}